#include <bits/stdc++.h>
using namespace std;

class Solution {
   public:
    void solve(int n, int m) {
        return;  // Default return
    }
};

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n, m;
        cin >> n >> m;

        Solution sol;
        sol.solve(n, m);
    }

    return 0;
}
